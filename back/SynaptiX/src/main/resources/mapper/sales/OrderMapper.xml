<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.sales.OrderMapper">
    <select id="selectAllOrders" resultType="com.example.demo.model.OrderDTO">
        SELECT 
            o.pk AS orderNo,
            o.Date AS orderDate,
            o.ProductId AS prodCode,
            o.ProductName AS prodName,
            o.Amount AS qty,
            o.Price AS unitPrice,
            o.Total AS amount,
            o.OrderStatus AS status
        FROM `order` o
    </select>
</mapper>